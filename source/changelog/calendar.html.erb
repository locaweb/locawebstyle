---
layout: changelog_layout
pageable: true
---
<% if paginate %>
	<div class="paginate">
		<p class="page_counter">página <%= page_number %> de <%= num_pages %></p>

		<% if prev_page %>
			<p class="page_control prev"><%= link_to '< Página anterior', prev_page %></p>
		<% end %>
	</div>
<% end %>

<h1>Arquivo de
  <% case page_type
     when 'day' %>
    <%= Date.new(year, month, day).strftime('%b %e %Y') %>
  <% when 'month' %>
    <%= Date.new(year, month, 1).strftime('%b %Y') %>
  <% when 'year' %>
    <%= year %>
  <% end %>
</h1>

<% page_articles.each_with_index do |article, i| %>
	<article>
		<h1><a href="<%= "#{base_url}/changelog/#{article.date.strftime('%Y/%m/%d')}/#{article.slug}" %>"><%= article.title %></a></h1>
		<div class="infoPost">
			<small><%= article.date.strftime('%Y/%m/%d') %></small>
		</div>
		<!-- use article.summary(250) if you have Nokogiri available to show just
		the first 250 characters -->
		<%= article.summary(250) %>
	</article>
	<hr>
<% end %>

<% if paginate %>
	<div class="paginate">
		<% if next_page %>
			<p class="page_control next"><%= link_to 'Próxima página >', next_page %></p>
		<% end %>
	</div>
<% end %>
